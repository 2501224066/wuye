<view class="goods">
  <view class="img">
    <image src="{{detail.img}}" mode="aspectFill"></image>
  </view>
  <view class="box">
    <view class="top">
      <view class="title">{{detail.title}}</view>
      <view class="memo">{{detail.memo}}</view>
    </view>
    <view class="price">
      <span>¥</span>{{detail.price}}
    </view>
  </view>
</view>